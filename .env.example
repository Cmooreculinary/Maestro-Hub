# Server-only Gemini API credential. Never prefix this with VITE_.
GEMINI_API_KEY="replace_with_your_key"

# Render injects PORT automatically. Local default: 3000.
PORT=3000

# Local default: ./data/maestro.db. Render disk mount: /var/data/maestro.db.
DATABASE_PATH="./data/maestro.db"
